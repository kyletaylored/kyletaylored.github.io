/** Pill-shaped tag/label for categories, tech stack, and status. */
export interface BadgeProps {
  label: string;
  /** Color variant */
  color?: 'default' | 'teal' | 'pink' | 'yellow' | 'blue' | 'red' | 'dark';
}
